---
import SearchWidget from './SearchWidget.astro';

const navLinks = [
	{ href: 'https://cc3po.com', label: 'Home' },
	{ href: 'https://offers.cc3po.com/our-services.html', label: 'Services' },
	{ href: '/blog', label: 'Blog' },
	{ href: 'https://insights.cc3po.com/lead-magnets.html', label: 'Resources' },
	{ href: 'https://audit.cc3po.com/', label: 'Free Audit' },
];
---

<style>
	nav {
		position: fixed;
		top: 0;
		left: 0;
		right: 0;
		height: 70px;
		background: rgba(10, 10, 11, 0.9);
		backdrop-filter: blur(20px);
		display: flex;
		align-items: center;
		border-bottom: 1px solid rgba(255, 255, 255, 0.06);
		z-index: 1000;
	}
	nav .inner {
		max-width: 1280px;
		margin: 0 auto;
		padding: 0 24px;
		width: 100%;
		display: flex;
		justify-content: space-between;
		align-items: center;
	}
	nav .logo {
		display: flex;
		align-items: center;
		text-decoration: none;
	}
	nav .logo img {
		height: 45px;
		width: auto;
	}
	.nav-center {
		display: flex;
		align-items: center;
		gap: 24px;
	}
	nav ul {
		display: flex;
		gap: 28px;
		list-style: none;
	}
	nav ul a {
		color: #a1a1aa;
		font-size: 14px;
		text-decoration: none;
		transition: color 0.2s;
	}
	nav ul a:hover {
		color: #fff;
	}
	nav ul a.active {
		color: #00d2ff;
	}
	.nav-right {
		display: flex;
		align-items: center;
		gap: 16px;
	}
	.nav-subscribe {
		background: linear-gradient(135deg, #6b35ff, #00d2ff);
		padding: 10px 20px;
		border-radius: 25px;
		color: #fff;
		font-size: 13px;
		font-weight: 600;
		text-decoration: none;
		transition: transform 0.2s, box-shadow 0.2s;
	}
	.nav-subscribe:hover {
		transform: translateY(-2px);
		box-shadow: 0 4px 15px rgba(107, 53, 255, 0.4);
	}
	.nav-toggle {
		display: none;
		flex-direction: column;
		gap: 5px;
		cursor: pointer;
		padding: 10px;
		position: absolute;
		right: 70px;
	}
	.nav-toggle span {
		width: 24px;
		height: 2px;
		background: #fff;
		transition: all 0.3s;
	}
	.mobile-menu {
		display: none;
		position: fixed;
		top: 70px;
		left: 0;
		right: 0;
		background: rgba(10, 10, 11, 0.98);
		border-bottom: 1px solid rgba(255, 255, 255, 0.06);
		padding: 20px;
		flex-direction: column;
		gap: 0;
		z-index: 999;
	}
	.mobile-menu.active {
		display: flex;
	}
	.mobile-menu a {
		display: block;
		padding: 14px 0;
		border-bottom: 1px solid rgba(255, 255, 255, 0.05);
		color: #a1a1aa;
		font-size: 16px;
		text-decoration: none;
	}
	.mobile-search {
		padding: 12px 0;
	}
	@media (max-width: 960px) {
		nav ul.nav-links {
			display: none;
		}
		.nav-toggle {
			display: flex;
		}
		.search-container {
			display: none;
		}
		.mobile-menu .search-container {
			display: block;
		}
	}
	@media (max-width: 768px) {
		nav .nav-links {
			display: none;
		}
		.nav-toggle {
			display: flex;
		}
	}
</style>

<nav>
	<div class="inner">
		<a href="https://cc3po.com" class="logo">
			<img src="http://cc3po.com/wp-content/uploads/2025/04/Color-logo-with-background-scaled.png" alt="CC3PO" style="height: 45px; width: auto;" />
		</a>
		<div class="nav-center">
			<ul class="nav-links">
				{navLinks.map(link => (
					<li>
						<a href={link.href} class:list={[Astro.url.pathname.startsWith(link.href) && 'active']}>
							{link.label}
						</a>
					</li>
				))}
			</ul>
		</div>
		<div class="nav-right">
			<SearchWidget />
			<a href="/blog#newsletter-section" class="nav-subscribe">Subscribe</a>
		</div>
	</div>
	<div class="nav-toggle" id="nav-toggle">
		<span></span>
		<span></span>
		<span></span>
	</div>
</nav>
<div class="mobile-menu" id="mobile-menu">
	<div class="mobile-search">
		<SearchWidget />
	</div>
	{navLinks.map(link => (
		<a href={link.href}>{link.label}</a>
	))}
	<a href="/blog#newsletter-section" class="nav-subscribe" style="text-align: center; margin-top: 12px;">Subscribe</a>
</div>

<script>
	document.getElementById('nav-toggle')?.addEventListener('click', () => {
		document.getElementById('mobile-menu')?.classList.toggle('active');
	});
</script>